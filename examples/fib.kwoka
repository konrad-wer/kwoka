fn fib(n)
{
  if n == 0 || n == 1 then
    n
  else
    fib(n - 1) + fib(n - 2)
}

fn map(f, xs)
{
  case xs of
    [] => []
    x : xs => f(x) : map(f, xs)
}

fn main()
{
  map(fib, [1,2,3,4,5,19])
}